type Role {
    _id: ID!
    name: String!
    ring: Int!
    users: [User] @relation(name: "HAS_ROLE" direction: "OUT")
}

type User {
    _id: ID!
    email: String!
    name: String
    roles: [Role]! @relation(name: "HAS_ROLE" direction: "IN")
}
